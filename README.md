# 🏋️ Training Service

Микросервис для управления процессом обучения/тренировок. Построен на базе **Spring Boot 3.4.1** и **Java 21**. Сервис реализует асинхронное взаимодействие через **Kafka**, кэширование через **Redis** и хранение данных в **PostgreSQL**.

---

## 🛠 Технологический стек

* **Java 21** (используются современные возможности языка).
* **Spring Boot 3.4.1**: Ядро приложения.
* **PostgreSQL**: Основная реляционная база данных.
* **Spring Data JPA**: Слой доступа к данным.
* **Redis**: Используется для кэширования и ускорения доступа к данным.
* **Apache Kafka**: Система обмена сообщениями (асинхронная обработка).
* **Lombok**: Сокращение шаблонного кода (boilerplate).
* **Springdoc OpenAPI (Swagger)**: Интерактивная документация API.
* **Docker Compose**: Автоматическое развертывание инфраструктуры.

---

## 🏗 Архитектура и ключевые особенности



### 1. Обработка данных (Kafka)
В проекте настроена кастомная фабрика слушателей (Kafka Listener Container Factory):
* **Batch Listening**: Чтение сообщений пачками для высокой производительности.
* **Manual Acknowledgment**: Ручное подтверждение смещений (offsets) для обеспечения гарантии доставки сообщений.

### 2. Слой данных и Кэширование
* Интегрирован **Redis** для кэширования часто запрашиваемых данных.
* Используется **Jackson JSR310** для корректной работы с датами (LocalDateTime) в JSON.

### 3. API и Документация
* Валидация входящих данных через **spring-boot-starter-validation**.
* Автоматическая генерация документации доступна по адресу после запуска.

---

## 🚀 Быстрый старт

### Предварительные требования
* Установленный **Docker** и **Docker Compose**.
* **JDK 21**.
* **Maven**.

### 1. Запуск инфраструктуры
Благодаря зависимости `spring-boot-docker-compose`, Spring Boot автоматически поднимет необходимые контейнеры (PostgreSQL, Redis, Kafka), если в корне проекта есть файл `docker-compose.yml`.

Если вы хотите запустить их вручную:
```bash
docker-compose up -d